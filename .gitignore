# 系统文件
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# 临时文件
*.tmp
*.bak
*.orig

# LaTeX 中间产物
*.aux
*.log
*.toc
*.out
*.synctex.gz

# Node 中间产物(如果以后用)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 用户配置
.git-commit-msg.txt
.git-remote-config.txt

# Git bundle 不进 git(从 git 导出,本就不该在 git 里)
*.bundle

# 大文件不进 git(GitHub 100MB 限制 + 不应该把证据包和 git 一起追踪)
_evidence/git-bundle-*.bundle
_evidence/evidence-package-FULL-*.tar.gz
